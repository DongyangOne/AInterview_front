import { TextInput } from "react-native";
import { View } from "react-native-reanimated/lib/typescript/Animated";

export const Input = () => {
  return (
    <View>
      <TextInput />
    </View>
  );
};
